[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forgetting-evidence"
version = "0.1.0"
description = "Backend primitives for verifiable machine-forgetting evidence"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
